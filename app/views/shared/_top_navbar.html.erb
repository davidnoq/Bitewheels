<nav class="navbar navbar-expand-md  bg-light text-dark" >
  <div class="container">
    <!-- Brand -->
    <a class="text-dark navbar-brand d-flex align-items-center" href="<%= root_path %>">
      <!-- You can replace the SVG below with your logo or brand icon -->
      <svg class="fs-5" width="40" height="32" role="img" aria-label="Bitewheels"><use xlink:href="#bitewheels-logo"/></svg>
      Bitewheels
    </a>

    <!-- Toggler for Mobile View -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible Navbar Content -->
    <div class="collapse navbar-collapse" id="navbarContent">
      <!-- Centered Navigation Links -->
      <ul class="navbar-nav mx-auto fs-6 mb-2 mb-md-0">
    
          <%if current_user%>
          <% if current_user.foodtruckowner? %>
            <li class="nav-item">
            
              <%= link_to "Food Trucks", food_trucks_path, class: "text-dark nav-link" %>
            </li>
            <li class="nav-item">
            <%= link_to "Applications", event_applications_path, class: "text-dark nav-link" %>
          </li>
        <li class="nav-item">
            <%= link_to "All Events", all_events_path, class: "text-dark nav-link" %>
             
            </li>
        <% else%>
        <%= link_to "My Events", events_path, class: "text-dark nav-link" %>
          <% end %>
         <% end %>
       
      </ul>

      <!-- Right-Aligned Authentication Buttons -->
      <div class="d-flex fs-6">
        <% if current_user %>
          <div class="dropdown ">
            <button class="text-white btn btn-dark dropdown-toggle" type="button" id="accountDropdown" data-bs-toggle="dropdown" aria-expanded="false">
              Account
            </button>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="accountDropdown">
              <li><%= link_to "View Profile", user_profile_path, class: " dropdown-item" %></li>
              <li>
                <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </li>
            </ul>
          </div>
        <% else%>
          <%= link_to "Log in", new_user_session_path, class: "btn btn-outline-dark me-2" %>
          <%= link_to "Sign up", new_user_registration_path, class: "btn btn-dark" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
