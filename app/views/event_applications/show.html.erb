<!-- app/views/event_applications/show.html.erb -->

<% content_for :title, "#{@event.name} - Application Details" %>

<div class="container py-5">
  <!-- Top Section with Heading and Icon -->
  <div class="mb-4 text-center">
  <h2 class="fw-bold text-dark d-flex align-items-center justify-content-center">
    <i class="bi bi-truck me-2"></i>
    <%= @food_truck.name %> - Application Details
  </h2>
  <hr class="my-4">

  

  <div class="col text-center mt-3 d-flex justify-content-center">
    <% if @event.accepting_applications && @event.approved_applications_count < @event.foodtruck_amount %>
      <%= button_to "Approve", approve_event_event_application_path(@event, @event_application), method: :patch, class: "btn btn-success me-2 btn-lg", data: { confirm: "Are you sure you want to approve this application?" }, aria: { label: "Approve Application" } %>
      <%= link_to "Back to Applications", event_event_applications_path(@event), class: "btn btn-secondary btn-lg me-2" %>
      <%= button_to "Reject", reject_event_event_application_path(@event, @event_application), method: :patch, class: "btn btn-danger me-2 btn-lg", data: { confirm: "Are you sure you want to reject this application?" }, aria: { label: "Reject Application" } %>
    <% elsif @event_application.status.in?(%w[pending approved]) %>
      <%= link_to "Back to Applications", event_event_applications_path(@event), class: "btn btn-secondary btn-lg me-2" %>
      <%= button_to "Reject", reject_event_event_application_path(@event, @event_application), method: :patch, class: "btn btn-danger me-2 btn-lg", data: { confirm: "Are you sure you want to reject this application?" }, aria: { label: "Reject Application" } %>
    <% else %>
      <%= link_to "Back to Applications", event_event_applications_path(@event), class: "btn btn-secondary btn-lg" %>
    <% end %>
  </div>
</div>



 
  
  <!-- Main Content Row -->
  <div class="row mt-5">
    <!-- Left Column: Event Info and Food Truck Info -->
    <div class="col-lg-6 mb-4">
      <!-- Application Details Card -->
      <div class="card mb-4 shadow-sm">
        
          <h5 class="card-title text-left ps-3 pt-3 mb-0"><%= @event_application.event.name %></h5>
        
        <div class="card-body">
          <!-- Event Information -->
          <p class="card-text">
            
            <strong>Address:</strong> <%= @event.address %><br>
            <strong>Dates:</strong> <%= @event.start_date.strftime("%B %d, %Y") %> - <%= @event.end_date.strftime("%B %d, %Y") %><br>
            <strong>Food Trucks Needed:</strong> <%= @event.foodtruck_amount %>
          </p>

          <!-- Application Status -->
          
        </div>
      </div>

      <!-- Accordion Section: Food Truck Information -->
      <div class="accordion shadow-sm" id="applicationAccordion">
        <div class="accordion-item">
          
          <div class="d-flex justify-content-between align-items-center">
            <h5 class="mb-3 p-2 d-flex align-items-center">
              <i class="bi bi-truck me-2"></i> <%= @food_truck.name %> Profile
            </h5>
            <span class="mb-3 p-2">
              <strong>Application Status: </strong>
              <% case @event_application.status %>
              <% when 'pending' %>
                <span class="badge bg-warning text-dark">Pending</span>
              <% when 'approved' %>
                <span class="badge bg-success">Approved</span>
              <% when 'rejected' %>
                <span class="badge bg-danger">Rejected</span>
              <% else %>
                <span class="badge bg-secondary"><%= @event_application.status.capitalize %></span>
              <% end %>
            </span>
          </div>
        </div>
        <!-- Food Truck Information -->
        <div class="accordion-item">
          <h2 class="accordion-header" id="headingOne">
            <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
              <i class="bi bi-info-circle me-2"></i> Food Truck Information
            </button>
          </h2>
          <div id="collapseOne" class="accordion-collapse collapse show" aria-labelledby="headingOne" data-bs-parent="#applicationAccordion">
            <div class="accordion-body">
              <p><strong>Cuisine:</strong> <%= @event_application.food_truck.cuisine %></p>
              
              <% if @event_application.food_truck.permit.attached? %>
                <div class="mb-3">
                  <h5>Permit</h5>
                  <%= image_tag url_for(@event_application.food_truck.permit), class: 'img-fluid img-thumbnail', alt: 'Permit', style: 'max-width: 300px; height: auto;' %>
                </div>
              <% else %>
                <p><em>No permit uploaded.</em></p>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Food Truck Reviews -->
        <div class="accordion-item">
          <h2 class="accordion-header" id="headingTwo">
            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
              <i class="bi bi-star me-2"></i> Food Truck Reviews
            </button>
          </h2>
          <div id="collapseTwo" class="accordion-collapse collapse" aria-labelledby="headingTwo" data-bs-parent="#applicationAccordion">
            <div class="accordion-body">
              <% if @food_truck.food_truck_ratings.any? %>
                <% @food_truck.food_truck_ratings.each do |rating| %>
                  <div class="mb-3">
                    <h5>
                      <%= rating.user.first_name %> <%= rating.user.last_name %> 
                      
                    </h5>
                    <p>
                      <% rating.rating.times do %>
                        <i class="bi bi-star-fill text-warning"></i>
                      <% end %>
                      <% (5 - rating.rating).times do %>
                        <i class="bi bi-star text-warning"></i>
                      <% end %>
                    </p>
                    <div class="d-flex justify-content-between align-items-center">
                      <p class="mb-0"><%= rating.review %></p>
                      <% if current_user == rating.user %>
                        <button class="btn btn-sm btn-primary edit-review-btn" data-bs-toggle="modal" data-bs-target="#editReviewModal<%= rating.id %>">
                          <i class="bi bi-pencil-square"></i> Edit
                        </button>
                      <% end %>
                    </div>
                    <hr>
                  </div>

                  <!-- Edit Review Modal for each rating -->
                  <div class="modal fade" id="editReviewModal<%= rating.id %>" tabindex="-1" aria-labelledby="editReviewModalLabel<%= rating.id %>" aria-hidden="true">
                    <div class="modal-dialog">
                      <div class="modal-content">
                        <div class="modal-header">
                          <h5 class="modal-title" id="editReviewModalLabel<%= rating.id %>">Edit Review</h5>
                          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">
                          <%= form_with(model: rating, url: food_truck_food_truck_rating_path(@food_truck, rating), method: :patch, local: true) do |f| %>
                            <div class="mb-3">
                              <%= f.label :rating, "Rating (0-5)", class: "form-label" %>
                              <%= f.number_field :rating, in: 0..5, step: 1, class: "form-control", required: true %>
                            </div>
                            <div class="mb-3">
                              <%= f.label :review, "Review", class: "form-label" %>
                              <%= f.text_area :review, rows: 4, class: "form-control", required: true %>
                            </div>
                            <div class="modal-footer">
                              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                              <%= f.submit "Update Review", class: "btn btn-primary" %>
                            </div>
                          <% end %>
                        </div>
                      </div>
                    </div>
                  </div>
                <% end %>
              <% else %>
                <p class="text-muted">No reviews yet for this food truck.</p>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Food Truck Menu -->
        <div class="accordion-item">
          <h2 class="accordion-header" id="headingThree">
            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseThree" aria-expanded="false" aria-controls="collapseThree">
              <i class="bi bi-list me-2"></i> Food Truck Menu
            </button>
          </h2>
          <div id="collapseThree" class="accordion-collapse collapse" aria-labelledby="headingThree" data-bs-parent="#applicationAccordion">
            <div class="accordion-body">
              <% if @event_application.food_truck.menu_images.any? %>
                <div class="row">
                  <% @event_application.food_truck.menu_images.each do |image| %>
                    <div class="col-md-4 mb-3">
                      <div class="card">
                        <%= image_tag url_for(image), class: 'card-img-top img-fluid', alt: 'Menu Image', style: 'height: 200px; object-fit: cover;' %>
                      </div>
                    </div>
                  <% end %>
                </div>
              <% else %>
                <p class="text-muted">No menu images available for this food truck.</p>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Food Images and Descriptions -->
        <div class="accordion-item">
          <h2 class="accordion-header" id="headingFour">
            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseFour" aria-expanded="false" aria-controls="collapseFour">
              <i class="bi bi-images me-2"></i> Food Images & Descriptions
            </button>
          </h2>
          <div id="collapseFour" class="accordion-collapse collapse" aria-labelledby="headingFour" data-bs-parent="#applicationAccordion">
            <div class="accordion-body">
              <% if @event_application.food_truck.food_images.any? %>
                <div class="row">
                  <% @event_application.food_truck.food_images.each_with_index do |image, index| %>
                    <div class="col-md-4 mb-3">
                      <div class="card">
                        <%= image_tag url_for(image), class: 'card-img-top img-fluid', alt: 'Food Image', style: 'height: 200px; object-fit: cover;' %>
                        <div class="card-body">
                          <p class="card-text">
                            <strong>Description:</strong> 
                            <%= @event_application.food_truck.food_image_descriptions[image.id.to_s].presence || "No description provided." %>
                          </p>
                        </div>
                      </div>
                    </div>
                  <% end %>
                </div>
              <% else %>
                <p class="text-muted">No food images uploaded for this food truck.</p>
              <% end %>
            </div>
          </div>
        </div>
      </div>

     
        <div class="card mt-4">
          <div class="card-header">
            <h5>Leave a Review and Rating</h5>
          </div>
          <div class="card-body">
            <%= form_with model: [@food_truck, @food_truck.food_truck_ratings.build], url: food_truck_food_truck_ratings_path(@food_truck), method: :post, local: true do |f| %>
              <div class="mb-3">
                <%= f.label :rating, "Rating (0-5)", class: "form-label" %>
                <%= f.number_field :rating, in: 0..5, step: 1, class: "form-control", required: true %>
              </div>
              <div class="mb-3">
                <%= f.label :review, "Review", class: "form-label" %>
                <%= f.text_area :review, rows: 4, class: "form-control", required: true %>
              </div>
              <%= f.submit "Submit Review", class: "btn btn-primary" %>
            <% end %>
          </div>
        </div>
      
    </div>

    <!-- Right Column: Chat Section -->
    <div class="col-lg-6 mb-4">
      <div class="card shadow-sm">
        <div class="card-header bg-dark text-white">
          <h5 class="mb-0 d-flex align-items-center">
            <i class="bi bi-chat-dots me-2"></i> Chat
          </h5>
        </div>
        <div class="card-body">
          <!-- Set the eventApplicationId for JavaScript -->
          <script>
            window.eventApplicationId = <%= @event_application.id %>;
          </script>

          <!-- Render the chat partial -->
          <%= render 'shared/chat', messages: @messages %>
        </div>
      </div>
    </div>
  </div>
</div>
